---
title: "10.VOUCH.Codex.Dev-Prompts.Legacy"
role: prompter
system:
workspace: vouch
type: reference
status: active
authority: reference
created: 2026-08-24
updated: 2026-08-24
tags: []
---
/
## Recommended path

Your repo is now **scaffold-heavy but implementation-light**. The fastest path is **not** “pick a page and build UI.” It is:

1. **Make the repo compile**

2. **Lock the persistence model**

3. **Implement auth/authz/readiness**

4. **Implement Stripe/Clerk/provider plumbing**

5. **Implement the Vouch lifecycle**

6. **Wire fetchers/actions into routes**

7. **Add dashboard/admin/UI**

8. **Backfill tests and acceptance gates**


That order matches the product’s hard dependencies: Vouch is server-authoritative; auth, authorization, validation, payment state, Vouch state, confirmation rules, audit events, database writes, and cache invalidation belong on the server, not client UI.

I also inspected the uploaded repo. It is structurally complete, but there is a practical blocker: many generated stubs contain invalid placeholder code like:

```ts
throw new Error($fn not implemented in ...)
```

So the first task must be **repo stabilization**, not feature implementation.

---

# Operating rules for Codex under rate limits

## Use one Codex session per task

Do **not** ask Codex to “finish the app.” That will burn tokens and produce a messy cross-domain diff.

Use this pattern:

```txt
Task N: one bounded milestone
Files to inspect: explicit list
Files allowed to edit: explicit list
Forbidden work: explicit list
Validation command: explicit list
Return: changed files + commands run + remaining errors
```

## Do not paste giant docs

Since the repo contains `.agents/`, tell Codex to read file paths directly. Provide **paths, not full contents**.

## Never provide `.env`

Codex can use the local repo and local env, but your prompt should explicitly say:

```txt
Do not print, copy, summarize, or modify .env values.
Do not expose secrets in logs, tests, comments, or output.
```

## Use “inspect first, patch second”

Every prompt should include:

```txt
First inspect the listed files and the current failing validation output.
Then make the smallest safe diff needed.
Do not rewrite working architecture.
```

---

# Task order

## 0. Repo stabilization / compile unblock

**Goal:** Make TypeScript parse, before implementing business logic.

**Why first:** I found hundreds of invalid placeholder throws and route placeholders. The repo cannot be safely improved until stubs compile.

**Provide Codex these files:**

```txt
AGENTS.md
package.json
tsconfig.json
eslint.config.mjs
next.config.ts
prisma/schema.prisma
lib/actions/*.ts
lib/fetcher/*.ts
lib/db/transactions/*.ts
lib/db/selects/*.ts
lib/db/mappers/*.ts
features/system/route-placeholder.tsx
```

**Prompt:**

```txt
You are working in the Vouch repo.

Objective:
Make the scaffold compile without implementing domain behavior.

Context:
- This is a scaffolded/stubbed Next.js App Router app.
- Do not implement real Vouch lifecycle logic yet.
- Do not modify .env or print secret values.
- Preserve the existing architecture and file structure.
- app/ must stay route-shell only.
- components must stay presentational.
- lib/actions and lib/fetcher may remain stubs, but they must be valid TypeScript.

Tasks:
1. Fix all invalid placeholder throws such as `throw new Error($fn not implemented...)`.
2. Replace them with valid, searchable placeholder errors.
3. Keep return types strict enough to avoid accidental runtime use.
4. Fix import/export syntax issues caused by scaffolding.
5. Do not remove required exported function names.
6. Do not build product features in this pass.

Validation:
Run:
- pnpm prisma:validate
- pnpm typecheck
- pnpm lint

Return:
- files changed
- commands run
- remaining errors, grouped by blocker
```

**Expected output:** Valid TS stubs. No product logic yet.

---

## 1. Prisma/schema/database integrity

**Goal:** Ensure the actual database model matches the domain contract.

Vouch requires canonical entities like `User`, `VerificationProfile`, `PaymentCustomer`, `ConnectedAccount`, `Vouch`, `Invitation`, `PresenceConfirmation`, `PaymentRecord`, `RefundRecord`, `TermsAcceptance`, `AuditEvent`, `NotificationEvent`, and `PaymentWebhookEvent`. The database instructions require those models, uniqueness constraints, indexes, safe provider references, and no marketplace/dispute models.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/domain-model.yaml
.agents/contracts/authz.yaml
.agents/contracts/roles.yaml
.agents/instructions/database-prisma.agents.md
prisma/schema.prisma
prisma.config.ts
lib/db/prisma.ts
package.json
```

**Prompt:**

```txt
Objective:
Validate and complete the Prisma persistence model for Vouch.

Read:
- AGENTS.md
- .agents/contracts/domain-model.yaml
- .agents/contracts/authz.yaml
- .agents/contracts/roles.yaml
- .agents/instructions/database-prisma.agents.md
- prisma/schema.prisma

Rules:
- Contract wins over code.
- No marketplace models.
- No dispute/arbitration models.
- No raw card data.
- No raw identity documents.
- No full Stripe/Clerk payload storage.
- Store provider references, statuses, timestamps, safe error codes only.
- Do not touch .env values.

Tasks:
1. Compare prisma/schema.prisma against the domain model contract.
2. Add/fix missing models, enums, relations, indexes, and uniqueness constraints.
3. Ensure webhook idempotency is represented.
4. Ensure invitation token is hash-only.
5. Ensure PresenceConfirmation uniqueness per vouch+role and vouch+user.
6. Ensure no forbidden models exist.
7. Add or update seed only if it already exists and is safe demo data.

Validation:
Run:
- pnpm prisma:validate
- pnpm prisma:generate
- pnpm typecheck

Return:
- schema changes
- any migration command needed
- any contract mismatch found
```

**Do not let Codex build UI here.**

---

## 2. Types + Zod schemas finalization

**Goal:** Lock runtime validation and domain vocabulary before actions.

The type/schema layer should contain domain types and Zod validation/sanitization schemas only, not DTOs, Prisma mirrors, transaction types, or provider payload shapes.

**Provide Codex:**

```txt

Objective:
Finalize Vouch domain types and Zod schemas.

Read:
- AGENTS.md
- .agents/contracts/domain-model.yaml
- .agents/contracts/features.yaml
- .agents/reports/types-schema.md
- types/*.ts
- schemas/*.ts

Rules:
- Use canonical Vouch terms only.
- Do not create DTOs or Prisma mirror types here.
- Use z.infer where practical.
- Server validation is authoritative.
- Preserve existing file shape.

Tasks:
1. Ensure schemas exist for createVouch, acceptVouch, declineVouch, confirmPresence, payment setup, payout setup, verification, settings, admin filters, dashboard query params, and webhooks.
2. Ensure createVouchInputSchema enforces:
   - amount minimum
   - usd only
   - confirmationOpensAt < confirmationExpiresAt
   - safe optional label
   - no service category / detailed purpose
3. Ensure invite token, returnTo, and internal path schemas are sanitized.
4. Ensure ActionResult type is consistent.
5. Export cleanly through types/index.ts and schemas/index.ts.

Validation:
Run:
- pnpm typecheck
- pnpm test -- --runInBand if available, otherwise pnpm test

Return:
- files changed
- schema names added/fixed
- any unresolved type issue
```

---

## 3. Auth + local user sync + authz

**Goal:** Make Clerk-backed identity map to local Vouch users and enforce server-side authorization.

The source requires auth helpers like `getCurrentUser`, `requireUser`, `requireActiveUser`, `getCurrentUserId`, `syncClerkUser`, and `getUserSetupStatus`; auth must resolve user, role, participant role, admin capability, verification readiness, payment readiness, payout readiness, and terms acceptance server-side.

**Provide Codex:**

```txt
Objective:
Implement Clerk-backed auth, local user sync, authz helpers, and Clerk webhook handling.

Relevant Files:
- AGENTS.md
- .agents/contracts/authz.yaml
- .agents/contracts/roles.yaml
- .agents/contracts/routes.yaml
- .agents/instructions/authz.agents.md
- .agents/reports/transactions-dto.md
- lib/auth/*
- lib/authz/*
- lib/fetcher/authFetchers.ts
- lib/actions/authActions.ts
- lib/db/transactions/authTransactions.ts
- lib/db/transactions/userTransactions.ts
- lib/db/selects/auth.selects.ts
- lib/db/selects/user.selects.ts
- lib/db/mappers/auth.mappers.ts
- lib/db/mappers/user.mappers.ts
- app/api/webhooks/clerk/route.ts

Rules:
- Do not expose or modify .env values.
- Do not use Clerk organizations.
- Do not create marketplace roles.
- Server-side authorization only.
- UI hiding is not authorization.
- Clerk webhook route must verify Svix signatures.
- Webhook processing must be idempotent.
- Do not store full Clerk payloads.

Tasks:
1. Implement current-user helpers.
2. Implement requireUser and requireActiveUser.
3. Implement syncClerkUser / ensureLocalUserForSession.
4. Implement authz helpers for:
   - admin
   - payer
   - accepted payee
   - invited candidate
   - participant access
5. Implement Clerk webhook handling for user.created, user.updated, user.deleted.
6. Ensure local User and default VerificationProfile are created/updated.
7. Write audit events where required.
8. Add focused tests for:
   - unauthenticated denied
   - disabled user denied
   - unrelated Vouch denied
   - self-accept blocked
   - admin required

Validation:
Run:
- pnpm prisma:validate
- pnpm typecheck
- pnpm test
```

---

## 4. Setup gates: verification, payment readiness, payout readiness, terms

**Goal:** Centralize eligibility logic before payment-bearing flows.

Create requires identity/adult/payment/terms; accept requires identity/adult/payout/terms; confirm requires active participant, active Vouch, open window, and no duplicate confirmation.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/authz.yaml
.agents/contracts/roles.yaml
.agents/instructions/host-onboarding.agents.md
.agents/reports/actions-fetchers.md
lib/auth/setup-gates.ts
lib/fetcher/setupFetchers.ts
lib/actions/setupActions.ts
lib/fetcher/verificationFetchers.ts
lib/actions/verificationActions.ts
lib/db/transactions/setupTransactions.ts
lib/db/transactions/verificationTransactions.ts
lib/db/selects/setup.selects.ts
lib/db/selects/verification.selects.ts
schemas/setup.ts
schemas/verification.ts
types/setup.ts
types/verification.ts
features/setup/*
features/settings/*
```

**Prompt:**

```txt
Objective:
Implement setup/readiness gates for Vouch payment-bearing flows.

Rules:
- Do not implement Stripe payment operations in this task.
- Do not implement marketplace onboarding.
- No public profile fields.
- Return paths must be internal-only.
- Server remains authoritative.

Tasks:
1. Implement setup gate calculation:
   - create_vouch
   - accept_vouch
   - confirm_presence
   - settings
2. Implement terms acceptance action.
3. Implement verification status fetchers/actions as provider-reference/status based.
4. Implement setup page state fetcher.
5. Ensure setup states support:
   - incomplete
   - complete
   - blocked by verification
   - blocked by payment
   - blocked by payout
   - return from invite
   - return from create
6. Add tests for gate calculation and internal return path validation.

Validation:
Run:
- pnpm typecheck
- pnpm test
```

---

## 5. Stripe customer, payment method, Connect payout, webhook shell

**Goal:** Build provider infrastructure without yet completing Vouch lifecycle.

Payment must use Stripe/Connect or equivalent provider-supported flows, store provider references/statuses, verify webhooks, and not claim platform custody.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/domain-model.yaml
.agents/contracts/authz.yaml
.agents/reports/transactions-dto.md
lib/integrations/stripe/*
lib/fetcher/paymentFetchers.ts
lib/actions/paymentActions.ts
lib/db/transactions/paymentTransactions.ts
lib/db/selects/payment.selects.ts
lib/db/mappers/payment.mappers.ts
app/api/webhooks/stripe/route.ts
schemas/payment.ts
types/payment.ts
```

**Prompt:**

```txt
Objective:
Implement Stripe payment/payout infrastructure and webhook idempotency shell.

Rules:
- Do not print or modify .env.
- Do not store raw Stripe payloads.
- Do not store raw card/bank data.
- Store provider IDs, status, timestamps, safe failure codes.
- Webhooks must verify Stripe signature.
- Webhooks must be idempotent.
- Business logic belongs in lib/integrations/stripe and lib/actions/paymentActions.ts, not route.ts.

Tasks:
1. Implement Stripe server client.
2. Implement customer/payment method setup session helpers.
3. Implement Connect onboarding/account helpers.
4. Implement payment/payout readiness refresh.
5. Implement Stripe webhook route delegation.
6. Implement PaymentWebhookEvent recording and duplicate-ignore behavior.
7. Add safe user-facing payment error mapping.
8. Add tests for idempotency and signature failure path if test utilities exist.

Validation:
Run:
- pnpm typecheck
- pnpm test
```

**Important:** This task should not decide the final capture/transfer architecture beyond a sandbox-safe abstraction unless you explicitly choose one.

---

## 6. Core Vouch lifecycle actions

**Goal:** Implement the money-moving product core.

The product requires create → accept → both confirm → release; otherwise refund/void/non-capture. One-sided confirmation must not release funds.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/domain-model.yaml
.agents/contracts/features.yaml
.agents/contracts/authz.yaml
.agents/contracts/acceptance-gates.yaml
.agents/instructions/booking.agents.md
.agents/reports/actions-fetchers.md
lib/actions/vouchActions.ts
lib/fetcher/vouchFetchers.ts
lib/vouch/*
lib/invitations/tokens.ts
lib/security/hash.ts
lib/security/idempotency.ts
lib/db/transactions/vouchTransactions.ts
lib/db/transactions/invitationTransactions.ts
lib/db/transactions/confirmationTransactions.ts
lib/db/transactions/paymentTransactions.ts
lib/db/transactions/auditTransactions.ts
lib/db/selects/vouch.selects.ts
lib/db/selects/invitation.selects.ts
lib/db/selects/confirmation.selects.ts
lib/db/mappers/vouch.mappers.ts
schemas/vouch.ts
types/vouch.ts
```

**Prompt:**

```txt
Objective:
Implement the core Vouch lifecycle actions.

Rules:
- Server action sequence: authenticate -> authorize -> validate -> transact/provider op -> audit -> revalidate -> typed result.
- One-sided confirmation never releases funds.
- Payer cannot accept own Vouch.
- Only one payee can accept.
- Invite token is hash-only.
- Confirmation must be within window.
- No dispute/arbitration/manual award behavior.
- No marketplace fields or service categories.

Implement:
1. createVouch
2. createVouchInvitation
3. send/resend/invalidate invitation if supported by queue stub
4. markInviteOpened
5. acceptVouch
6. declineVouch
7. cancelPendingVouch
8. confirmPresence
9. evaluateAggregateConfirmation
10. completeVouchIfBothConfirmed
11. expireUnresolvedVouches
12. refundExpiredVouch
13. markVouchFailed

Tests:
- create eligible payer success
- setup blocked create failure
- accept valid invite success
- self-accept denied
- second payee denied
- confirm outside window denied
- duplicate confirmation denied
- one-sided confirmation waits
- both confirmations trigger completion path
- expired unresolved triggers refund/non-capture path

Validation:
Run:
- pnpm prisma:validate
- pnpm typecheck
- pnpm test
```

This is the highest-value implementation task. Spend Codex budget here.

---

## 7. Fetchers, DTOs, dashboard, detail state

**Goal:** Read paths and UI state models.

Fetchers must authenticate/authorize, query minimal selects, map to DTO/read models, apply cache policy, and return only what the consuming feature needs.

**Provide Codex:**

```txt
AGENTS.md
.agents/reports/actions-fetchers.md
.agents/reports/transactions-dto.md
.agents/instructions/guest-inbox.agents.md
.agents/instructions/host-dashboard.agents.md
lib/fetcher/vouchFetchers.ts
lib/fetcher/dashboardFetchers.ts
lib/fetcher/settingsFetchers.ts
lib/db/selects/*.ts
lib/db/mappers/*.ts
types/dashboard.ts
types/settings.ts
types/vouch.ts
schemas/dashboard.ts
schemas/settings.ts
```

**Prompt:**

```txt
Objective:
Implement participant-scoped read models and dashboard/detail fetchers.

Rules:
- Fetchers must authenticate and authorize.
- No raw Prisma objects returned to features.
- No protected overfetching.
- Dashboard shows only payer/payee participant Vouches.
- Invite candidate summaries belong to invite route, not broad dashboard queries.
- URL search params drive filters/sort/pagination.

Tasks:
1. Implement listUserVouches/listPayerVouches/listPayeeVouches.
2. Implement getVouchDetailForParticipant.
3. Implement confirmation/payment/timeline safe summaries.
4. Implement dashboard summary and sections.
5. Implement settings/readiness cards.
6. Add tests for participant scope, unrelated denied, filter parsing, DTO shape.

Validation:
Run:
- pnpm typecheck
- pnpm test
```

---

## 8. Route and feature wiring

**Goal:** Replace placeholders with real feature composition.

Your route inventory is already large: public, auth, setup, payment/payout, dashboard, create, invite/accept, Vouch detail, confirmation, settings, admin, notifications, and system states.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/routes.yaml
.agents/reports/component-mapping.md
.agents/reports/page-views.md
app/**/page.tsx
features/marketing/*
features/auth/*
features/setup/*
features/payments/*
features/dashboard/*
features/vouches/*
features/settings/*
components/layout/*
components/vouches/*
components/setup/*
components/payments/*
components/forms/*
```

**Prompt:**

```txt
Objective:
Wire implemented fetchers/actions into app routes and feature pages.

Rules:
- app/ files remain thin route shells.
- Feature files orchestrate page composition.
- Components remain pure/presentational.
- Do not implement new routes beyond routes.yaml.
- Do not add forbidden marketplace/dispute routes.
- Do not fetch protected data inside components.

Tasks:
1. Replace RoutePlaceholder usage for implemented pages only.
2. Wire dashboard, setup, settings, vouches index, create, invite, detail, confirm.
3. Preserve loading/error/not-found boundaries.
4. Use existing shared components.
5. Keep client components only where forms/interactions require them.
6. Add route-level tests if existing pattern supports it.

Validation:
Run:
- pnpm typecheck
- pnpm lint
- pnpm test
```

---

## 9. Admin operational visibility

**Goal:** Admin reads operational state without arbitration.

Admins may inspect users, Vouches, payments, refunds, webhook events, notification events, audit events, and failure states, but must not decide disputes, award funds, force release, rewrite confirmation truth, collect claims, message users, or assign blame.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/authz.yaml
.agents/contracts/roles.yaml
.agents/instructions/admin-moderation.agents.md
lib/fetcher/adminFetchers.ts
lib/actions/adminActions.ts
lib/db/transactions/adminTransactions.ts
lib/db/selects/admin.selects.ts
lib/db/mappers/admin.mappers.ts
features/admin/*
components/admin/*
app/(admin)/*
```

**Prompt:**

```txt
Objective:
Implement admin operational views only.

Rules:
- Admin is operational, not arbitral.
- No force release.
- No award funds.
- No confirmation rewrite.
- No dispute/evidence/claim tooling.
- Safe retries must be idempotent and audited.
- Non-admins denied server-side.

Tasks:
1. Implement admin dashboard fetcher.
2. Implement admin Vouch/payment/user/audit/webhook list/detail fetchers.
3. Implement admin route protection.
4. Implement safe retry only for technical idempotent operations if existing infrastructure supports it.
5. Wire admin pages and tables.
6. Add tests:
   - non-admin denied
   - admin can read operational records
   - forbidden actions/components/routes absent

Validation:
Run:
- pnpm typecheck
- pnpm lint
- pnpm test
```

---

## 10. Notifications + analytics

**Goal:** Add lifecycle visibility without scope creep.

Notifications include invite, accepted, confirmation window open, confirmation recorded, completed, expired/refunded, and payment failed. Analytics should remain lifecycle/funnel-oriented, not marketplace surveillance.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/analytics.yaml
.agents/reports/actions-fetchers.md
lib/notifications/*
lib/integrations/email/*
lib/actions/notificationActions.ts
lib/actions/analyticsActions.ts
lib/db/transactions/notificationTransactions.ts
lib/db/transactions/analyticsTransactions.ts
schemas/notification.ts
schemas/analytics.ts
types/notification.ts
types/analytics.ts
```

**Prompt:**

```txt
Objective:
Implement notification queue and privacy-minimized lifecycle analytics.

Rules:
- No sensitive meeting-purpose text.
- No marketplace analytics.
- No profile views, search queries, reviews, messages, disputes.
- Notifications should use safe templates.
- Failed notification attempts should be recorded safely.

Tasks:
1. Implement queue notification actions.
2. Implement email template rendering for MVP lifecycle events.
3. Implement notification delivery status updates.
4. Implement analytics event writers for approved lifecycle events only.
5. Wire notifications from Vouch lifecycle actions if not already wired.
6. Add tests for queued, sent, failed, skipped.

Validation:
Run:
- pnpm typecheck
- pnpm test
```

---

## 11. Final acceptance-gate pass

**Goal:** Prove MVP local integrity.

The acceptance gates require contract integrity, domain model integrity, no marketplace surface, no arbitration surface, auth enforcement, create/accept/manual confirmation, deterministic resolution, audit events, payment sandbox, webhook idempotency, route access control, setup gates, notification lifecycle, admin operational visibility, and E2E smoke depending on milestone.

**Provide Codex:**

```txt
AGENTS.md
.agents/contracts/acceptance-gates.yaml
.agents/execution/validation.json
package.json
playwright.config.ts
vitest.config.ts
tests if present
app/*
features/*
lib/*
components/*
prisma/schema.prisma
```

**Prompt:**

```txt
Objective:
Run and repair the MVP local acceptance gates.

Rules:
- Fix only issues required to pass validation.
- Do not add new features.
- Do not loosen tests to pass.
- Do not delete required acceptance checks.
- Do not expose .env.

Run:
- pnpm validate
- pnpm test:e2e

Tasks:
1. Fix lint/type/test failures.
2. Add missing focused tests for acceptance gates.
3. Verify forbidden routes/components/models are absent.
4. Verify authz and participant scope tests pass.
5. Verify create/accept/confirm/expire/refund tests pass or are properly scaffolded with provider mocks.
6. Update .agents/execution/validation.json with truthful status only if that is the repo convention.

Return:
- pass/fail status
- files changed
- commands run
- remaining blockers
```

---

# Token-minimized Codex prompt template

Use this for every task:

```txt
Task: <one sentence>

Read only these context files first:
- AGENTS.md
- <specific .agents contract/instruction>
- <specific source files>

Allowed edit scope:
- <specific files/folders>

Forbidden:
- Do not modify .env or print secrets.
- Do not add marketplace, discovery, profiles, reviews, messages, categories, disputes, evidence, or manual award/release behavior.
- Do not move business logic into app/ or components/.
- Do not return raw provider payloads or raw Prisma objects to UI.

Execution:
1. Inspect current files.
2. Make the smallest safe implementation.
3. Preserve exports and architecture.
4. Add/update focused tests where appropriate.
5. Run the listed validation commands.

Validation:
- <commands>

Return:
- changed files
- commands run
- remaining errors
```

---

# What not to give Codex

Do **not** paste:

```txt
.env
full repo tree
all .agents docs
entire PRD
all mockup/page inventory files
old chat transcript
all source files
screenshots unless task is UI-specific
```

Do give:

```txt
AGENTS.md
1-3 relevant .agents files
target files/folders
validation commands
specific acceptance criteria
```

---

# Practical milestone plan

## Milestone A — Compile + DB

Finish:

```txt
0. Repo stabilization
1. Prisma/schema/database integrity
2. Types + Zod schemas
```

Exit criteria:

```bash
pnpm prisma:validate
pnpm prisma:generate
pnpm typecheck
```

## Milestone B — Server authority

Finish:

```txt
3. Auth + authz
4. Setup gates
5. Stripe/Clerk provider shells
```

Exit criteria:

```bash
pnpm typecheck
pnpm test
```

## Milestone C — Core product loop

Finish:

```txt
6. Vouch lifecycle actions
7. Fetchers/DTO/dashboard/detail
```

Exit criteria:

```bash
pnpm test
```

Required green-path flow:

```txt
sign up/sync user
complete readiness
create Vouch
open invite
accept Vouch
confirm payer
confirm payee
complete/release path recorded
```

Required failure flow:

```txt
create Vouch
accept Vouch
only one confirms OR nobody confirms
expiration triggers refund/void/non-capture path
```

## Milestone D — UI and operations

Finish:

```txt
8. Route/feature wiring
9. Admin operational visibility
10. Notifications/analytics
11. Final acceptance pass
```

Exit criteria:

```bash
pnpm validate
pnpm test:e2e
```

---

# My strongest recommendation

Use Codex first on **Task 0 only**. Do not let it implement auth, Prisma, and Vouch lifecycle in the same session.

The repo already has enough surface area that a broad prompt will create expensive, low-signal diffs. The winning pattern is **small vertical tasks with ruthless file scoping**:

```txt
Stabilize compile → Prisma → schemas → authz → gates → payments → lifecycle → fetchers → UI → admin → tests
```

That is the lowest-token, lowest-rework path.