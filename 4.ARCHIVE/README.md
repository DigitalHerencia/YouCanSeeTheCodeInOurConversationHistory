# Archive

Archive is a PARA state, not a second classification system. Set `archived: true` through Meta Bind and move inactive durable material here when useful. Do not use tags such as `status/archived` to duplicate lifecycle state.
