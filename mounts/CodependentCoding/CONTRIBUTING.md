# Contributing

Changes follow the active `context/specs/LV-###` specification and linked GitHub Issue. Use one reviewable branch and pull request per bounded outcome, preserve `template/` as the sole canonical application source, and attach fresh executed validation evidence.

Run `corepack pnpm validate` before review. Template changes additionally require generated-output acceptance through the real CLI.
