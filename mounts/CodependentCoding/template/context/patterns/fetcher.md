# Fetcher

An exported protected fetcher validates untrusted read input, resolves the actor and legal tenant scope, uses explicit selects, and returns mapped DTOs. It is self-securing and has no write or redirect side effects.
