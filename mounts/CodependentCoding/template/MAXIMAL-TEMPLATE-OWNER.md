# Maximal Template™ source owner

This directory is the single active runnable Maximal Template superset used by
the local reconciliation. It was migrated from the semantically matching
`src/TheMaximalTemplate-main` source. The former `src/` trees remain only as
historical migration evidence and are not runtime dependencies.
