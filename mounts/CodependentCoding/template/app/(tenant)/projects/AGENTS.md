# Projects sample domain

Routes validate identifiers, fetch authorized DTOs, and render project features. Pages and components never import Prisma. Server Actions receive mutations; workflows enforce resource policies and lifecycle invariants.
