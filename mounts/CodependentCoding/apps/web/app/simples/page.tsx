import { CanonicalSurface } from '@/components/canonical-surface';

export default function SimplesPage() {
  return (
    <CanonicalSurface
      eyebrow="Two normalized families"
      title="The Simples™"
      description="PureUI Blocks render the presentation layer. BusinessLogic Blocks coordinate domain workflows. Their constituents remain in the architecture layer that owns them."
      image="/Simples Logo.jpg"
      nextHref="/docs"
      nextLabel="Read TypeScripture"
    />
  );
}
