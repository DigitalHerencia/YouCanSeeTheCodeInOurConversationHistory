# Segment 0 Plan

```yaml
Segment: 0 - Authority overlay and migration map
Outcome: Establish current authority, canonical source ownership, migration destinations, and a durable checkpoint before structural implementation changes.
Existing implementation to reuse: The shared Hipster Stack schema/core/CLI, CodependentCoding-site, both Maximal Template imports, root presentation source, Codebase Context Utility explorer components, and the embedded Loaded Vibes plugin payload.
Files/owners to change: Root and machine governance, current execution records, the Segment 0 migration map, and the Segment 0 checkpoint.
Compatibility adapters (if any): Imported src/* paths, four legacy presets, /libraries, /configure, old package paths, and the installed .agents plugin copy remain temporarily and receive explicit removal segments.
Focused validation: Parse execution JSON, confirm every mapped source path exists, confirm canonical authority references, and inspect the resulting governance diff.
Deletion/removal deferred until: Web paths after Segment 1/7 redirects exist; documentation after Segment 2; preset adapters after Segment 3/6; duplicate Maximal and explorer apps after Segment 5; installed plugin duplication after Segment 8; remaining stale wrappers after Segment 9.
```
