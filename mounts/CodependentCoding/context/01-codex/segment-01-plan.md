# Segment 1 Plan

```yaml
Segment: 1 - Product identity, shell, and Visual Vibes foundation
Outcome: Make apps/web the active Codependent Coding product, expose Ontologies | Simples | Anthimeria | Maximal navigation, and establish the shared dark industrial Visual Vibes shell over supplied assets.
Existing implementation to reuse: The current Next 16 CodependentCoding-site, its shared-core browser adapter, existing landing composition, UI button primitive, root brand assets, and the nested Hipster Stack schema/core/CLI packages required by the web build.
Files/owners to change: Move the web app to apps/web; move nested schema/core/cli packages to canonical package roots without semantic rewrites; move brand assets into apps/web/public; update root workspace metadata, web shell/landing, route aliases, CSS tokens/base layout, and the product/architecture machine contracts; compare the landing against docs/mockups/Landing Mockup.jpg at 894x1600.
Compatibility adapters (if any): /libraries and /configure become redirects; the current libraries and Constituter feature implementations remain internal migration source for Segments 3/4/7; four preset semantics remain unchanged until Segment 3/6.
Focused validation: Install the locked workspace dependencies, run root typecheck plus the web typecheck/build, inspect generated routes, and smoke canonical/legacy navigation at desktop and narrow widths when a browser runtime is available.
Deletion/removal deferred until: Legacy libraries/Constituter internals after Segments 4/7; imported Maximal/explorer sources until Segment 5; the nine non-landing mockups remain acceptance evidence for their owning segments.
```
