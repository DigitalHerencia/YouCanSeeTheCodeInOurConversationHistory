# Source Package Disposition

| Supplied source | Disposition in this handoff |
|---|---|
| `TYPESCRIPTURE-Reified-One-to-One-Edition.zip` | Extracted in full under `10-authority/typescripture/`. Current handoff identity map outranks stale names embedded in older chapters. |
| `Ontologies-Authority-Package(1).zip` | Extracted under `10-authority/ontologies/`; canonical source for nine normalized defaults and their file/relationship inventory. |
| `Simples-Authority-Package(1).zip` | Extracted under `10-authority/simples/`; canonical source for Simples = PureUI + BusinessLogic. |
| `Anthimeria-Authority-Package(1).zip` | Extracted under `10-authority/anthimeria/`; canonical source for stateless workbench and presentation-first configuration boundary. |
| `Codependent-Coding-Generation-System-Authority-Bundle(1).zip` | Extracted under `10-authority/generation/`; canonical source for Hipster Stack, Virgule, Maximal Template, Arrangement, and generation pipeline. |
| `Loaded-Vibes-Codex-Plugin-Arrangement-v0.1.0(1).zip` | Canonical docs/build evidence copied under `10-authority/loaded-vibes/`; operational code already exists in the workspace and must be preserved/moved rather than reimplemented. |
| `Codependent-Coding-Docs-Knowledge-Authority-Package(1).zip` | Superseded as a standalone knowledge authority by TypeScripture. Preserved under `90-superseded-source/` only for provenance/IA clues; it cannot override TypeScripture. |
| `TheCodependentCodingWebAppArchitecture.zip` | Current implementation evidence and consolidation workspace. |
| ten JPG mockups | Already embedded in workspace `context/mockups/`; visual/interaction acceptance reference, not semantic authority. |

## Important rule

A lower-authority source can still contain the best existing implementation. **Authority determines what the thing means; implementation evidence determines what useful code already exists.** Codex should reconcile the two rather than choosing between “keep everything old” and “rewrite everything.”
