# Security

Report vulnerabilities privately through GitHub security advisories. Do not open a public Issue containing credentials or exploit details.

The V1 initializer never requests provider credentials, provisions resources, or deploys. Generated projects contain examples only. Target paths are treated as untrusted and generation uses a run-owned sibling staging directory.
