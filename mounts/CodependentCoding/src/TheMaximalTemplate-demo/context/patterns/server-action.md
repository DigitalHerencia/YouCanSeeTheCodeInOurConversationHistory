# Server Action

A Server Action adapts browser mutation intent: validate input, resolve a trusted actor through the workflow, invoke one named use case, and return a stable serializable result. It does not contain the use case or import Prisma.
