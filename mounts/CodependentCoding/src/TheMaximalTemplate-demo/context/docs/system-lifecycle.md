# System lifecycle

Every durable transition has one named authority, legal preconditions, and a persistence strategy. Provider mirrors reconcile external truth. Webhooks are verified and claimed idempotently. Recovery and audit state never replace canonical domain state.
