# Tenant routes

Resolve the trusted actor and active organization before protected work. Routes stay thin, features orchestrate presentation, fetchers own reads, Server Actions receive mutations, and application-owned authorization preserves the tenant boundary.
