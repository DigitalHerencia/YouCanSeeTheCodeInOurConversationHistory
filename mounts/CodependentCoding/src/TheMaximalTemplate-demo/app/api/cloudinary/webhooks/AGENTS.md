# Cloudinary notifications

Verify `X-Cld-Signature` against the raw body and timestamp, runtime-validate the payload, claim the event idempotently, resolve the repository-owned asset binding, and reconcile tenant state.
