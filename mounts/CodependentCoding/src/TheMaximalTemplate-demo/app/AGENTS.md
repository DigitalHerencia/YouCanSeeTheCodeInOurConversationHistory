# Application routes

Routes adapt framework input and compose features. Keep protected reads in self-securing fetchers, mutations in thin Server Actions, use-case coordination in workflows, and persistence in transaction helpers. Route groups do not create URL segments; avoid colliding pages.
