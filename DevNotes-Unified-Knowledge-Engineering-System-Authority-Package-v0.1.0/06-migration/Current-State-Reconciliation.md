# Current-State Reconciliation

Observed current roots include the legacy numbered categories, `.obsidian`, current `AGENTS.md`, `DevNotes.base`, and existing Codependent/ChatGPT project structures.

Relevant current tool state:
- Hearth is installed and starts as the dashboard.
- Meta Bind has no reusable control templates.
- Callout Studio already has the intended semantic vocabulary.
- Note Toolbar has no default/folder mappings.
- Templater uses `90 OBSIDIAN/Templates` and one automatic capture mapping.
- `DevNotes.base` already demonstrates useful backlink/outgoing-link formulas.
- custom CSS exists but is not enabled.

These facts guide migration. They do not silently redefine the target.
