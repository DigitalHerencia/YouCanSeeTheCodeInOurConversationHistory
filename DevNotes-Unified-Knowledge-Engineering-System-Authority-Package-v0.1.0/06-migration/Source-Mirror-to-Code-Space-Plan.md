# Source Mirror → Code Space

Pilot one trusted repository under `_mounts/`.

Classify each mirror artifact:

```text
pure live-code duplicate          → replace candidate
extracted architecture/knowledge  → promote/preserve
provenance/historical snapshot    → preserve
unique annotation                 → preserve/promote
obsolete generated wrapper        → deletion candidate
```

No deletion is included in this package.
