# DevNotes Unified AGENTS.md — Proposed Target

DevNotes is a source-controlled knowledge-and-engineering system.

## Primary classifier

```text
Organization/lifecycle?         → Chief of Staff
Evidence/verification?          → Trust Issues
Implementation/function?        → Execution
Architecture/topology/platform? → Vibes
Knowledge/classification?       → DevNotes
Ontology/model/schema?          → Schemes
Language/prompt/semantics?      → Prömpter
Business/external reality?      → Fuck You Pay Me
```

Workspace/project ownership is a facet, not a root taxonomy.

## Knowledge model
Use the shared concepts: Epistemology, Ontology, Terminology, Taxonomy, Typology, Mereology, Topology, Nomenclature, Semantics, Schema, Metadata, Folksonomy, Faceted Classification, Information Architecture, Domain Model, Knowledge Graph.

Do not redefine a concept outside its canonical role owner.

## Codependent Coding
For software work adopting Codependent Coding, preserve its accepted architecture, boundaries, and terminology. Live repository state controls implementation facts.

## Authority
Authority is fact-specific. Operational state cannot silently override canon. Machine contracts cannot silently override human canon when they drift.

## Migration
Until migration is explicitly complete:
- legacy paths remain valid;
- no note deletion;
- no mass metadata rewrite;
- preserve path-sensitive links/queries;
- report structural changes exactly.

## Obsidian
`.obsidian/` is runtime config.
`_obsidian/` is durable user-owned tooling.
`_mounts/` is local-only live-code access.

## Evidence
Never report an unrun check as passed.

## Final rule
Role determines primary ownership. Workspace and system are facets. Obsidian recomposes the graph.
