---
title: Maximal Template™ / Arrangement™ — Current Implementation Evidence Catalog
type: implementation-evidence
scope: repository
project: Codependent Coding
domain: maximal-template
artifact: evidence-catalog
namespace: codependentcoding.maximal-template.evidence
status: active
authority: implementation-evidence
repository: DigitalHerencia/TheMaximalTemplate
branch: main
observed: 2026-08-22
---

# Maximal Template™ / Arrangement™ — Current Implementation Evidence Catalog

# 1. Live Root Evidence

Inspected root includes:

```text
.agents/
.github/
AGENTS.md
ARCHITECTURE.md
README.md
app/
components/
content/
context/
features/
...
```

The repository is a real Next.js/TypeScript application, consistent with the “runnable superset” doctrine.

---

# 2. Live Architecture Map

Current `ARCHITECTURE.md` says the deeper human architecture contract lives in:

```text
context/docs/architecture.md
```

and repository governance/source precedence in:

```text
AGENTS.md
```

Numbered implementation scope lives under:

```text
context/specs/
```

The root architecture map is explicitly a compatibility map, not the ultimate authority.

---

# 3. Current Responsibility Evidence

Observed responsibility table:

```text
Static public presentation  → app/(public) + components/blocks
Application orchestration   → features
React Hook Form             → Feature form + components/ui
Persisted read              → lib/fetchers
Ordinary CRUD write         → lib/actions
Prisma select               → lib/db/selects
DTO mapper                  → lib/db/dto
Atomic database helper      → lib/db/transactions
Clerk authentication        → lib/auth
RBAC/ABAC/resource policy   → lib/authz
Provider behavior           → lib/integrations/{provider}
Webhook HTTP lifecycle      → app/api/{provider}/.../route.ts
Remaining domain logic      → lib/workflows/{domain}
```

This is strong evidence of the target decomposition.

---

# 4. Current README Evidence

The live README describes:

- one coherent maximal implementation;
- not nine separate starter applications;
- public demo edition inspectable while signed out;
- route → feature → block → primitive presentation;
- feature → library application behavior;
- nine recipe/domain families;
- generation by selecting a subset of one maximal application.

Those implementation concepts align strongly with the new Domain Library role.

---

# 5. Transitional Differences

The live README still describes older roles:

```text
Codependent Coding™ Knowledge System
Loaded Vibes™ Web App Architecture
Hipster Stack™ generator/CLI
```

Canonical current roles are:

```text
Codependent Coding™ WebApp Architecture
Hipster Stack™ Technology Stack / generator
Maximal Template™ Domain Library
Loaded Vibes™ Codex plugin
```

The technical implementation evidence remains valuable.

The identity language must be reconciled.
